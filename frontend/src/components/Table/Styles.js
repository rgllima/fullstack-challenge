export default function tableStyles(theme) {
  return {
    box: {
      height: 400,
      width: '100%'
    },
    dataGrid: {
      color: "#BB86FC",
      "& p, .MuiSvgIcon-root": {
        color: "#BB86FC",
        fill: "#BB86FC"
      }
    }
  }
}
